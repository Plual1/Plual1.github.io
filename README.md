# Plual1.github.io

Portfolio site structure:
- `/` renders the project list in `index.html`
- `/project-placeholder/` is a placeholder project directory
- `/project-placeholder/privacy/` is that project's privacy page

To rename the placeholder project, update the `PROJECTS` list in `/index.html` and rename `/project-placeholder`.
